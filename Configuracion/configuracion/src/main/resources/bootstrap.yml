server:
  port: 8300

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/atarax-ia/Microservicios.git
          search-paths: config_data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  application:
    name: configuracion
  security:
    user:
      name: root
      password: s3cr3t
